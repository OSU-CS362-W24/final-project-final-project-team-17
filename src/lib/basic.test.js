/*
    This is a basic test to satisfy jest, and not break the CI/CD pipeline.
    This should be deleted by whoever is writing unit tests, or with git pull later on.
*/

test('Basic test -- DELETE ME', () => {
    const value = 5;

    expect(value).toBe(5);
})